services:
  - type: web
    name: springboot-backend
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/shop-0.0.1-SNAPSHOT.war
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod